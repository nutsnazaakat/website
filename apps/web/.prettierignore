# Seed data uses one-line object rows deliberately.
# Prettier would explode each row across multiple lines, which is harder to scan
# and breaks the `grep -c '^  { slug:'` count checks used to verify seed integrity.
src/mocks/

# Vendored shadcn/ui primitives — kept byte-comparable to upstream so they can be
# diffed against a future shadcn version. Not ours to reformat.
src/components/ui/

# Generated by the TanStack Router plugin.
src/routeTree.gen.ts

# The design contract. Ported verbatim from LOVABLE REFERENCE / and verified
# byte-identical apart from one @source line. Reformatting it — even harmlessly —
# breaks that guarantee and makes future reference diffs meaningless.
src/styles/index.css

# Build output
dist/
